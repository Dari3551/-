PORT=5050
# Postgres (Docker compose)
DATABASE_URL="postgresql://chatkick:chatkick@localhost:5432/chatkick?schema=public"
JWT_SECRET="dev_super_secret_change_later"
CORS_ORIGIN="http://localhost:5173"

# SMTP (اختياري) — إن لم تضبطه: سيتم طباعة الكود في كونسول السيرفر للتجربة
SMTP_HOST=""
SMTP_PORT=587
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="ChatKick <no-reply@chatkick.local>"
